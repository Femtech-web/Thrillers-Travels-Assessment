/* eslint-disable no-unused-vars */

import styles from '../../GenStyles';

const style = {
    icon: `${styles.flexCenter} w-8 absolute top-[60%] left-2`,
    inputCont: 'flex flex-col mt-4 relative text-[0.9rem]',
    input: 'border-2 border-black/10 mt-2 focus:border-btnColor text-sm px-2 py-3 pl-12 outline-none focus:border-2 focus:bg-blue-50 rounded-md'
}

export default style;